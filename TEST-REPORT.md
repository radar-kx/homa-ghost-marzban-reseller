# گزارش آزمون نسخه 0.1.0

تاریخ: 2026-08-13

## نتیجه اجراشده در محیط ساخت

| کنترل | نتیجه |
|---|---|
| Parse همه فایل‌های PHP با Parser مستقل | PASS |
| توازن بلوک‌های Blade (`if`, `foreach`, `forelse`, `auth`) | PASS |
| توازن تگ‌های Form در همه Viewها | PASS |
| وجود همه Viewهای فراخوانی‌شده از Controllerها | PASS |
| وجود Routeهای فراخوانی‌شده در Blade | PASS |
| اعتبار JSON فایل Composer | PASS |
| Syntax اسکریپت Bash نصب cPanel | PASS |
| توازن آکولادهای CSS و بارگذاری JS | PASS |
| جستجوی `eval`, `shell_exec`, `passthru`, Raw SQL و Debug dump | PASS — موردی یافت نشد |
| کنترل عدم وجود `.env`، توکن یا Credential واقعی در بسته | PASS |

## تست‌های Runtime موجود در بسته

- تبدیل دقیق GB به Byte و عدد صحیح پول؛
- رد URL ناامن، HTTP و IP خصوصی برای اتصال مرزبان؛
- Idempotency شارژ/برداشت کیف پول؛
- رد برداشت با موجودی ناکافی؛
- برابری Debit و Credit دفترکل؛
- جلوگیری از دسترسی نماینده به مسیر مدیر؛
- جلوگیری از ورود حساب غیرفعال؛
- جلوگیری از دو بار شارژ در تأیید تکراری رسید؛
- ساخت سرویس با Marzban API شبیه‌سازی‌شده و جلوگیری از کسر دوباره.

## محدودیت این گزارش

PHP و Composer در محیط ساخت حاضر نبودند؛ بنابراین PHPUnit و Boot کامل Laravel در این محیط اجرا نشد. پس از نصب وابستگی‌ها روی هاست آزمایشی، پیش از استفاده واقعی این فرمان باید PASS شود:

```bash
php artisan test
```

همچنین تست اتصال مرزبان باید از پنل مدیر اجرا و یک اکانت آزمایشی کم‌حجم ساخته و تمدید شود. تا عبور این دو مرحله، این بسته MVP قابل نصب است اما نباید «آماده Production نهایی» تلقی شود.
